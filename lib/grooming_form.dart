import 'package:flutter/material.dart';

class GroomingForm extends StatefulWidget {
  @override
  _GroomingFormState createState() => _GroomingFormState();
}

class _GroomingFormState extends State<GroomingForm> {
  // Tambahkan kode Anda untuk menangani penginputan pengguna
  // dan menampilkan jadwal pemangkasan hewan peliharaan

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: EdgeInsets.all(16.0),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: <Widget>[
          // Tambahkan widget form Anda di sini
        ],
      ),
    );
  }
}
